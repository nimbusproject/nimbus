Settings for the Nimbus elastic interfaces hosted in GT4.0.x which serve as an
IaaS gateway service.  This is used with an implementation of the Nimbus RM API
that secures VMs from EC2 instead of managing a local VMM cluster
(see ../ec2client/README.txt).
